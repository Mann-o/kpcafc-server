'use strict'

/*
|---------------------------------------------------------------
| Redis Subscribers
|---------------------------------------------------------------
| Assumes listeners are stored inside `app/Listeners` directory.
*/

// const Redis = use('Redis')

// Redis.subscribe('news', async () => {
// })

// Redis.subcribe('news', 'News.onMessage')
